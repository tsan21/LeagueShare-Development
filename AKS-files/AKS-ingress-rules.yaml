apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: ingress-rules-keycloak
  namespace: leagueshare
  annotations:
    kubernetes.io/ingress.class: nginx
    nginx.ingress.kubernetes.io/ssl-redirect: "false"
    nginx.ingress.kubernetes.io/force_no_ssl_redirect: "true"
    # nginx.ingress.kubernetes.io/backend-protocol: "HTTP"
    # nginx.ingress.kubernetes.io/use-regex: "true"
    # nginx.ingress.kubernetes.io/rewrite-target: /
spec:
  rules:
  - http:
      paths:
      - path: /auth
        pathType: Prefix
        backend:
          service:
            name: keycloak
            port:
              number: 8080
---

apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: ingress-rules-imgur
  namespace: leagueshare
  annotations:
    kubernetes.io/ingress.class: nginx
    nginx.ingress.kubernetes.io/ssl-redirect: "false"
    nginx.ingress.kubernetes.io/force_no_ssl_redirect: "true"
spec:
  rules:
  - http:
      paths:
      - path: /keycloak
        pathType: Prefix
        backend:
          service:
            name: imgur-service
            port:
              number: 8091  
---

apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: ingress-rules-user
  namespace: leagueshare
  annotations:
    kubernetes.io/ingress.class: nginx
    nginx.ingress.kubernetes.io/ssl-redirect: "false"
    nginx.ingress.kubernetes.io/force_no_ssl_redirect: "true"
    nginx.ingress.kubernetes.io/use-regex: "true"
spec:
  rules:
  - http:
      paths:
      - path: /(.*)
        pathType: Prefix
        backend:
          service:
            name: user-service
            port:
              number: 8092 