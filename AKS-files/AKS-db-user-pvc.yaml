apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  labels:
    app: db-user
  name: db-user
  namespace: leagueshare
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 100Mi