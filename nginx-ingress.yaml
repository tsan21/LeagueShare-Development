apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: nginx-ingress
  annotations:
    kubernetes.io/ingress.class: nginx
    nginx.ingress.kubernetes.io/rewrite-target: /$1
spec:
  rules:
  # - http:
  #     paths:
  #     - backend:
  #         serviceName: keycloak
  #         servicePort: 8080
  #       path: /auth/*
  #     - backend:
  #         serviceName: imgur-service
  #         servicePort: 8091
  #       path: /keycloak/*
  # - host: http://127.0.0.1:56435/sso/login
      # - host: hello-world.info
    - host: hello-world.info
      http:
        paths:
          - path: /auth/*
            pathType: Prefix
            backend:
              service:
                name: keycloak
                port:
                  number: 8080
          - path: /keycloak/*
            pathType: Prefix
            backend:
              service:
                name: imgur-service
                port:
                  number: 8091                